name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: echo "Building Zava-vendor-portal..."
      - name: Test
        run: echo "Testing Zava-vendor-portal... All tests passed."
